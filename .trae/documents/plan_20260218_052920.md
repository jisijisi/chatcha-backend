I will align the "regeneration cache modal animation" in `admin.html` to match `index.html`.

My analysis shows that while the HTML structure and JS logic are largely identical, the CSS in `admin.html` uses hardcoded colors (e.g., `#fff`, `#64748b`) whereas `index.html` uses theme-aware CSS variables (e.g., `var(--bg-card)`, `var(--text-muted)`). This prevents `admin.html` from adapting to themes correctly and creates a maintenance drift.

**Plan:**
1.  **Read** the exact CSS block for `.magic-container` and related animations from `index.html`.
2.  **Update** `admin.html` by replacing its inline CSS block (approx. lines 65-364) with the theme-aware CSS from `index.html`.
3.  **Verify** that `admin.css` defines the necessary CSS variables (`--bg-card`, `--text-muted`, etc.) so the new styles render correctly. (I have already confirmed `admin.css` has these variables).

This will ensure visual consistency and code alignment between the two pages.