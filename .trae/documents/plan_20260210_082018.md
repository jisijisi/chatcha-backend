I will implement the ability for admins to edit AI prompts directly from the admin panel. The backend infrastructure is already in place, so the work will focus on the frontend implementation.

### 1. Frontend: Create Prompts Module

Create a new file `frontend/assets/js/admin/modules/prompts.js` that will:

* Fetch all prompts from the API (`GET /api/admin/prompts`).

* Render a list of available prompts (General, Knowledge Base, Personal Action, etc.).

* Provide an interface to edit the `system_prompt` for each type.

* Handle saving changes (`PUT /api/admin/prompts/:type`).

* Handle resetting to default (`POST /api/admin/prompts/:type/reset`).

### 2. Frontend: Update Navigation & Routing

Modify `frontend/assets/js/admin/core/navigation.js` to:

* Add a `prompts` entry to `VIEW_CONFIG`.

* Configure the loader to import and setup the new `prompts.js` module.

### 3. Frontend: Update Admin HTML

Modify `frontend/admin.html` to:

* Add a new sidebar navigation item for "AI Prompts" (likely with a suitable icon).

* Add a new view container `<div id="prompts-view" class="view-section">` that will hold the prompt management UI.

### 4. Frontend: Register Module

Modify `frontend/assets/js/admin/main.js` to:

* Import and initialize the new `prompts` module during the application startup sequence.

### 5. Verification

* Verify that the "AI Prompts" link appears in the admin sidebar.

* Verify that clicking it loads the list of prompts.

* Verify that editing a prompt and saving it updates the database.

* Verify that the "Reset to Default" functionality works.

