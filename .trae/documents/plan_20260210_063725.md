I will implement skeleton loading animations for the Admin Panel's Permissions Modal to improve the user experience during data fetching.

### 1. Update `permissions.js`
I will modify `frontend/assets/js/admin/modules/permissions.js` to replace the simple "Loading permissions..." text with a rich skeleton structure that mirrors the actual content.

**Changes:**
1.  **Create `renderPermissionSkeleton()` function:**
    - This function will generate HTML using the existing `.skeleton` CSS classes found in `admin.css`.
    - It will create a layout simulating the "Knowledge Base" and "Live Data Tools" sections.
    - It will use `skeleton-title` for section headers and repeated blocks for categories/sources.

2.  **Update `openPermModal()`:**
    - Call `renderPermissionSkeleton()` immediately when the modal opens, before the asynchronous `apiFetch` calls complete.
    - This ensures the user sees the loading state immediately.

### Implementation Details
The skeleton structure will look like this:
```html
<div class="perm-section-wrapper" style="border: 1px solid #e2e8f0; border-radius: 8px; margin-bottom: 20px;">
  <div style="padding: 16px 20px; border-bottom: 1px solid #e2e8f0;">
    <div class="skeleton skeleton-title" style="width: 150px; margin-bottom: 8px;"></div>
    <div class="skeleton skeleton-text" style="width: 300px;"></div>
  </div>
  <div style="padding: 12px;">
    <!-- Repeated Skeleton Rows -->
    <div class="skeleton" style="height: 50px; margin-bottom: 8px; border-radius: 6px;"></div>
    <div class="skeleton" style="height: 50px; margin-bottom: 8px; border-radius: 6px;"></div>
    <div class="skeleton" style="height: 50px; margin-bottom: 8px; border-radius: 6px;"></div>
  </div>
</div>
<!-- Repeat for second section -->
```

This reuses the existing CSS and matches the visual weight of the actual content.